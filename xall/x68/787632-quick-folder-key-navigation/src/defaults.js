// Default settings. Settings missing from storage.local wil be initialised to these values.
const defaultSettings = {
  // MailfolderKeyNav: should mail folder key navigation be turned on 
  // (true) or off (false).
  MailFolderKeyNav: true,
  // MailfolderKeyNavMenuItem: should a checkbox allowing MailFolderKeyNav 
  // to be toggled on/off be placed on the mail folder context menu (true)
  // or not (false).
  MailFolderKeyNavMenuItem: true
}
